The Docker images for the VNF packages must be already installed and the VNF and NS packages also must be already uploaded in Open Source Mano.

Once everything is ready, Just run the script start.sh

When everything is finished, to destroy te scenario just run the script destroy.sh
